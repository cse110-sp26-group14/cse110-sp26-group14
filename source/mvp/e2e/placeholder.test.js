/**
 * E2E placeholder — replace with Playwright browser tests later.
 */
describe('E2E placeholder', () => {
  test('reserves CI slot for future browser tests', () => {
    expect(true).toBe(true);
  });
});
