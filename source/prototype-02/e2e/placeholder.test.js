/**
 * E2E placeholder — keeps CI job structure until Playwright (or similar) is added.
 * Replace with real browser tests (hash routes, check-in modal, etc.) in a later sprint.
 */
describe("E2E placeholder", () => {
  test("smoke: pipeline slot reserved for future browser tests", () => {
    expect(true).toBe(true);
  });
});
