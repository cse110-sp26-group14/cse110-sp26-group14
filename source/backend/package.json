{
  "name": "se-sitrep-backend",
  "version": "2.0.0",
  "private": true,
  "description": "SE SitRep API — Vanilla JS (Node), SQLite, OpenAI",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "start:env": "node --env-file=.env server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  }
}
