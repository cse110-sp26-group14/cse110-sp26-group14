{
  "name": "se-sitrep-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.js",
    "db:local": "wrangler d1 execute se-sitrep --local --file=./schema.sql && wrangler d1 execute se-sitrep --local --file=./seed.sql",
    "db:remote": "wrangler d1 execute se-sitrep --remote --file=./schema.sql && wrangler d1 execute se-sitrep --remote --file=./seed.sql"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
